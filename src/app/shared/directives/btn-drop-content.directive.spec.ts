import { BtnDropContentDirective } from './btn-drop-content.directive';

describe('BtnDropContentDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnDropContentDirective();
    expect(directive).toBeTruthy();
  });
});
